**Day-1**

```assets/images/Search_User.png```

A. Create a simple home page with top naviagation menu as per the UI

B. Create a grid view with list of customer information to be displayed

```dart
import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Rife Center',
      home: UserListScreen(),
    );
  }
}

class UserListScreen extends StatelessWidget {
  final List<Map<String, dynamic>> users = List.generate(
    10,
    (index) => {
      'leftName': 'John Doe',
      'leftEmail': 'john.doe@gmail.com',
      'rightName': 'John Hobbs',
      'rightEmail': 'john.hobbs@gmail.com',
    },
  );

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Row(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
             Image.asset("assets/logo_rife.png", height: 33, width: 33,),
             SizedBox(width: 20,),
            Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text('Rife Center', style: TextStyle(color: Colors.black, fontWeight: FontWeight.bold)),
                Text('Richlands, VA', style: TextStyle(color: Colors.grey, fontSize: 12)),
              ],
            ),
            
          ],
        ),
        backgroundColor: Colors.white,
        elevation: 0,
        actions: <Widget>[
          Container(
            height: 43,
            width: 43,
            decoration: BoxDecoration(
              color: const Color.fromARGB(255, 201, 198, 198),
              borderRadius: BorderRadius.circular(6)
            ),
            child: IconButton(
              icon: Icon(Icons.person_add_alt_1, color: Colors.black, size: 20,),
              onPressed: () {
                // Settings action
              },
            ),
          ),
          SizedBox(width: 10,),
          Container(
            height: 43,
            width: 43,
            decoration: BoxDecoration(
              color: const Color.fromARGB(255, 201, 198, 198),
              borderRadius: BorderRadius.circular(6)
            ),
            child: IconButton(
              icon: Icon(Icons.edit_document, color: Colors.black, size: 20,),
              onPressed: () {
                // QR code scanner action
              },
            ),
          ),
          SizedBox(width: 10,),
          Container(
            height: 43,
            width: 43,
            decoration: BoxDecoration(
              color: const Color.fromARGB(255, 201, 198, 198),
              borderRadius: BorderRadius.circular(6)
            ),
            child: IconButton(
              icon: Icon(Icons.notifications, color: Colors.black,size: 20,),
              onPressed: () {
                
              },
            ),
          ),
          SizedBox(width: 10,)
        ],
      ),
      body: Column(
        children: <Widget>[
          Container(
            color: Colors.blue.shade50, // Background color for the tab area
            child: Row(
              mainAxisAlignment: MainAxisAlignment.spaceAround,
              children: <Widget>[

                TextButton(
                  
                  onPressed: () {
                    // Search User action
                  },
                  child: Row(
                    children: [
                      Icon(Icons.person_outline_outlined, color: Colors.black, size: 20,),
                      SizedBox(width: 10,),
                      Text('Search User', style: TextStyle(color: Colors.blue)),
                    ],
                  ),
                ),
                TextButton(
                  onPressed: () {
                    // Appointments action
                  },
                  child: Row(
                    children: [
                      Icon(Icons.perm_contact_calendar_rounded, color: Colors.black,size: 20,),
                      SizedBox(width: 10,),
                      Text('Appointments', style: TextStyle(color: Colors.black)),
                    ],
                  ),
                ),
                TextButton(
                  onPressed: () {
                    // Rooms action
                  },
                  child: Row(
                    children: [
                      Icon(Icons.local_hospital_sharp, size: 20,color: Colors.black,),
                      SizedBox(width: 10,),
                      Text('Rooms', style: TextStyle(color: Colors.black)),
                    ],
                  ),
                ),
              ],
            ),
          ),
          Padding(
            padding: const EdgeInsets.all(8.0),
            child: TextField(
              decoration: InputDecoration(
                prefixIcon: Icon(Icons.search),
                hintText: 'Search Users',
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(2),
                ),
              ),
            ),
          ),
          Expanded(
            child: ListView.builder(
              itemCount: users.length,
              itemBuilder: (context, index) {
                return Row(
                  children: <Widget>[
                    Expanded(
                      child: UserCard(
                        name: users[index]['leftName'],
                        email: users[index]['leftEmail'],
                      ),
                    ),
                    Expanded(
                      child: UserCard(
                        name: users[index]['rightName'],
                        email: users[index]['rightEmail'],
                      ),
                    ),
                  ],
                );
              },
            ),
          ),
        ],
      ),
    );
  }
}

class UserCard extends StatelessWidget {
  final String name;
  final String email;

  const UserCard({Key? key, required this.name, required this.email}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: ListTile(
        title: Text(name),
        subtitle: Text(email),
        trailing: Icon(Icons.arrow_forward_ios),
      ),
    );
  }
}
```
